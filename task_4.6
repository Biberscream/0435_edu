import java.util.Scanner;
public class Main {

    public static void main(String[] args) {
        Scanner scan = new Scanner(System.in);
        int x;
        int y = scan.nextInt();
        if (y % 400 == 0) x=366;
        else if (y % 100 == 0) x=365;
        else if (y % 4 == 0) x=366;
        else x=365;
        System.out.println("Количество дней в году: " + x);
        }
}
